{
  "name": "degendogs-functions",
  "private": true,
  "main": "lib/index.js",
  "type": "commonjs",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "@farcaster/quick-auth": "^0.0.8",
    "firebase-functions": "^4.4.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "5.8.3"
  }
}
