rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TODO: tighten rules before production. This is an open ruleset.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
